import fs from 'node:fs';

function read(path) { return fs.readFileSync(path, 'utf8'); }
function write(path, content) { fs.writeFileSync(path, content); }
function replace(path, before, after) {
  const src = read(path); const count = src.split(before).length - 1;
  if (count !== 1) throw new Error(`${path}: expected one match, got ${count}\n${before}`);
  write(path, src.replace(before, after));
}
function replaceRe(path, re, after) {
  const src = read(path); if (!re.test(src)) throw new Error(`${path}: no match ${re}`); re.lastIndex = 0; write(path, src.replace(re, after));
}

// #61 — a site is semantically distinct when relocation/binding metadata differs.
replace('js/binary/model.js', `        const key = \`${'${s.address ?? \'\'}'}:${'${s.offset ?? \'\'}'}:${'${s.kind ?? \'\'}'}\`;`, `        const scalar = (value) => typeof value === 'bigint' ? value.toString() : value == null ? '' : String(value);\n        const key = [scalar(s.address), scalar(s.offset), s.kind || '', scalar(s.type), scalar(s.addend), scalar(s.pointerFormat), s.weak ? '1' : '0'].join(':');`);

// #62 — emit syntactically valid signed hexadecimal BigInt strings.
replace('js/binary/model.js', `      if (typeof v === 'bigint') return '0x' + v.toString(16).toUpperCase();`, `      if (typeof v === 'bigint') return v < 0n ? '-0x' + (-v).toString(16).toUpperCase() : '0x' + v.toString(16).toUpperCase();`);

// #63/#64 — recover legacy thread entrypoints and deployment targets.
replace('js/binary/macho.js', `const LC_DYSYMTAB = 0xb;`, `const LC_THREAD = 0x4;\nconst LC_UNIXTHREAD = 0x5;\nconst LC_DYSYMTAB = 0xb;`);
replace('js/binary/macho.js', `const LC_MAIN = 0x80000028;\nconst LC_BUILD_VERSION = 0x32;`, `const LC_VERSION_MIN_MACOSX = 0x24;\nconst LC_VERSION_MIN_IPHONEOS = 0x25;\nconst LC_MAIN = 0x80000028;\nconst LC_VERSION_MIN_TVOS = 0x2f;\nconst LC_VERSION_MIN_WATCHOS = 0x30;\nconst LC_BUILD_VERSION = 0x32;`);
replace('js/binary/macho.js', `      else if (cmd === LC_MAIN && cmdsize >= 24) linkeditData.main = { entryoff: r.u64(p + 8), stacksize: r.u64(p + 16) };\n      else if (cmd === LC_FUNCTION_STARTS`, `      else if (cmd === LC_MAIN && cmdsize >= 24) linkeditData.main = { entryoff: r.u64(p + 8), stacksize: r.u64(p + 16) };\n      else if ((cmd === LC_THREAD || cmd === LC_UNIXTHREAD) && cmdsize >= 16) {\n        const pc = parseThreadEntrypoint(r, p, cmdsize, cpu, bits);\n        if (pc != null && linkeditData.threadEntry == null) linkeditData.threadEntry = pc;\n      }\n      else if (cmd === LC_VERSION_MIN_MACOSX || cmd === LC_VERSION_MIN_IPHONEOS || cmd === LC_VERSION_MIN_TVOS || cmd === LC_VERSION_MIN_WATCHOS) parseLegacyVersionMin(r, p, cmd, image);\n      else if (cmd === LC_FUNCTION_STARTS`);
replace('js/binary/macho.js', `  if (linkeditData.main) {\n    image.entrypoint = image.offsetToAddress(linkeditData.main.entryoff);\n    if (image.entrypoint != null) image.functions.push(functionSeed(image.entrypoint, { source: 'entrypoint', confidence: 0.9 }));\n  }`, `  if (linkeditData.main) {\n    image.entrypoint = image.offsetToAddress(linkeditData.main.entryoff);\n    image.metadata.entrypointSource = 'LC_MAIN';\n  } else if (linkeditData.threadEntry != null) {\n    image.entrypoint = linkeditData.threadEntry;\n    image.metadata.entrypointSource = 'LC_UNIXTHREAD';\n  }\n  if (image.entrypoint != null && image.entrypoint !== 0n) image.functions.push(functionSeed(image.entrypoint, { source: 'entrypoint', confidence: 0.9 }));`);
replace('js/binary/macho.js', `function parseBuildVersion(r, p, image) {\n  const platform = r.u32(p + 8);\n  const minos = r.u32(p + 12);\n  const sdk = r.u32(p + 16);\n  image.metadata.buildVersion = { platform, platformName: platformName(platform), minos: version32(minos), sdk: version32(sdk) };\n  image.platform = platformName(platform) || image.platform;\n}`, `function parseBuildVersion(r, p, image) {\n  const platform = r.u32(p + 8);\n  const minos = r.u32(p + 12);\n  const sdk = r.u32(p + 16);\n  image.metadata.buildVersion = { platform, platformName: platformName(platform), minos: version32(minos), sdk: version32(sdk), source: 'LC_BUILD_VERSION' };\n  image.platform = platformName(platform) || image.platform;\n}\n\nfunction parseLegacyVersionMin(r, p, cmd, image) {\n  if (image.metadata.buildVersion?.source === 'LC_BUILD_VERSION') return;\n  const platform = cmd === LC_VERSION_MIN_MACOSX ? 1 : cmd === LC_VERSION_MIN_IPHONEOS ? 2 : cmd === LC_VERSION_MIN_TVOS ? 3 : 4;\n  image.metadata.buildVersion = { platform, platformName: platformName(platform), minos: version32(r.u32(p + 8)), sdk: version32(r.u32(p + 12)), source: 'LC_VERSION_MIN' };\n  image.platform = platformName(platform) || image.platform;\n}\n\nfunction parseThreadEntrypoint(r, p, cmdsize, cpu, bits) {\n  const end = p + cmdsize;\n  let q = p + 8;\n  while (q + 8 <= end) {\n    const flavor = r.u32(q);\n    const count = r.u32(q + 4);\n    const state = q + 8;\n    const stateBytes = count * 4;\n    if (!Number.isSafeInteger(stateBytes) || stateBytes < 0 || state + stateBytes > end) return null;\n    const arch = cpuName(cpu);\n    if (arch === 'arm64' && flavor === 6 && stateBytes >= 272) return r.u64(state + 264);\n    if (arch === 'x86_64' && flavor === 4 && stateBytes >= 136) return r.u64(state + 128);\n    if (arch === 'arm' && bits === 32 && flavor === 1 && stateBytes >= 64) return BigInt(r.u32(state + 60));\n    q = state + stateBytes;\n  }\n  return null;\n}`);

// #73 — compressed chained symbol pools must be explicitly partial, never silently "parsed".
replace('js/binary/macho-dyld.js', `  image.metadata.chainedFixups = { version, startsOffset, importsCount, importsFormat, symbolsFormat };\n  if (symbolsFormat !== 0) {\n    image.warnings.push(\`chained-fixups symbol pool format ${'${symbolsFormat}'} is not supported\`);\n    return null;\n  }`, `  image.metadata.chainedFixups = { version, startsOffset, importsCount, importsFormat, symbolsFormat, complete: true, symbolsComplete: true };\n  if (symbolsFormat !== 0) {\n    image.metadata.chainedFixups.complete = false;\n    image.metadata.chainedFixups.symbolsComplete = false;\n    image.metadata.chainedFixups.partialReason = symbolsFormat === 1 ? 'compressed-symbol-pool' : 'unknown-symbol-pool-format';\n    image.warnings.push(\`chained-fixups symbol pool format ${'${symbolsFormat}'} is not supported; results are partial\`);\n    return null;\n  }`);

// #72 — support the generic 32-bit chained bind format and diagnose every unsupported format.
replace('js/binary/macho-dyld.js', `          const off = image.addressToOffset(address);\n          if (off == null || off + 8n > BigInt(r.length)) break;\n          const raw = r.u64(Number(off));\n          const d = decodeChainedPointer(raw, pointerFormat);\n          if (!d) break;`, `          const off = image.addressToOffset(address);\n          const width = chainedPointerWidth(pointerFormat);\n          if (!width) { markUnsupportedChainedFormat(image, pointerFormat); break; }\n          if (off == null || off + BigInt(width) > BigInt(r.length)) break;\n          const raw = width === 4 ? BigInt(r.u32(Number(off))) : r.u64(Number(off));\n          const d = decodeChainedPointer(raw, pointerFormat);\n          if (!d) { markUnsupportedChainedFormat(image, pointerFormat); break; }`);
replace('js/binary/macho-dyld.js', `function decodeChainedPointer(raw, format) {\n  if (format === 2 || format === 6) {`, `function chainedPointerWidth(format) {\n  if ([3, 4, 5].includes(format)) return 4;\n  if ([1, 2, 6, 7, 9, 10, 12].includes(format)) return 8;\n  return 0;\n}\nfunction markUnsupportedChainedFormat(image, format) {\n  image.metadata.chainedFixups ||= {};\n  image.metadata.chainedFixups.complete = false;\n  image.metadata.chainedFixups.bindingSitesComplete = false;\n  const list = image.metadata.chainedFixups.unsupportedPointerFormats ||= [];\n  if (!list.includes(format)) { list.push(format); image.warnings.push(\`chained pointer format ${'${format}'} is not supported; binding sites are partial\`); }\n}\nfunction decodeChainedPointer(raw, format) {\n  if (format === 3) {\n    const bind = !!((raw >> 31n) & 1n);\n    const next = Number((raw >> 26n) & 0x1fn);\n    if (!bind) return { bind: false, ordinal: -1, addend: 0n, next, stride: 4 };\n    const ordinal = Number(raw & 0xfffffn);\n    let addend = Number((raw >> 20n) & 0x3fn);\n    if (addend & 0x20) addend -= 0x40;\n    return { bind: true, ordinal, addend: BigInt(addend), next, stride: 4 };\n  }\n  if (format === 2 || format === 6) {`);

// #65-#69 — replace the classic bind interpreter with bounded, status-carrying decoding.
replaceRe('js/binary/macho-dyld.js', /export function parseClassicBindings\([\s\S]*?\n\}\n\nexport function parseExportTrie/, `export function parseClassicBindings(r, dc, image, segments, source) {\n  if (!dc || !dc.size || dc.offset + dc.size > r.length) return null;\n  const BIND_OPCODE_MASK = 0xf0, BIND_IMMEDIATE_MASK = 0x0f;\n  const ptrSize = image.bits === 64 ? 8n : 4n;\n  let p = dc.offset;\n  const end = dc.offset + dc.size;\n  let libOrdinal = 0, symbol = '', symbolFlags = 0, type = 1, addend = 0n, segIndex = 0, segOffset = 0n;\n  let threadedTable = null, threadedTableLimit = 0;\n  const status = { source, complete: true, decodedBinds: 0, threadedApplies: 0, unsupportedOpcodes: [] };\n  image.metadata.dyldBindings ||= { complete: true, streams: {} };\n  image.metadata.dyldBindings.streams[source] = status;\n  const fail = (message, opcode = null) => {\n    status.complete = false; image.metadata.dyldBindings.complete = false;\n    if (opcode != null && !status.unsupportedOpcodes.includes(opcode)) status.unsupportedOpcodes.push(opcode);\n    image.warnings.push(\`${'${source}'}: ${'${message}'}\`);\n  };\n  const snapshotImport = () => ({ name: symbol, library: dylibForOrdinal(image, libOrdinal), ordinal: libOrdinal, weak: !!(symbolFlags & 1), symbolFlags, nonWeakDefinition: !!(symbolFlags & 8), addend, type, source, sites: [] });\n  const validLocation = () => {\n    const seg = segments[segIndex];\n    return !!seg && segOffset >= 0n && segOffset <= seg.size && ptrSize <= seg.size - segOffset;\n  };\n  const bind = () => {\n    if (!symbol) return;\n    if (threadedTable && threadedTable.length < threadedTableLimit) { threadedTable.push(snapshotImport()); return; }\n    if (!validLocation()) { fail(\`bind location is outside segment ${'${segIndex}'} at +0x${'${segOffset.toString(16)}'}\`); return; }\n    const seg = segments[segIndex];\n    const address = seg.address + segOffset;\n    const imp = snapshotImport();\n    imp.sites.push({ address, offset: image.addressToOffset(address), kind: source, type, addend, weak: imp.weak });\n    image.imports.push(imp); status.decodedBinds++;\n  };\n  const applyThreaded = () => {\n    if (!threadedTable) { fail('threaded APPLY encountered before ordinal table'); return; }\n    if (!validLocation()) { fail('threaded APPLY starts outside its segment'); return; }\n    const seg = segments[segIndex];\n    let address = seg.address + segOffset;\n    for (let guard = 0; guard < 100000; guard++) {\n      const off = image.addressToOffset(address);\n      if (off == null || off + 8n > BigInt(r.length)) { fail('threaded binding chain leaves mapped file data'); return; }\n      const raw = r.u64(Number(off));\n      const isBind = !!((raw >> 62n) & 1n);\n      const delta = Number((raw >> 51n) & 0x7ffn);\n      if (isBind) {\n        const ordinal = Number(raw & 0xffffn);\n        const template = threadedTable[ordinal];\n        if (!template) fail(\`threaded bind ordinal ${'${ordinal}'} is outside table\`);\n        else {\n          const imp = { ...template, sites: [{ address, offset: off, kind: 'threaded-bind', type: template.type, addend: template.addend, weak: template.weak }] };\n          image.imports.push(imp); status.decodedBinds++;\n        }\n      }\n      if (!delta) { status.threadedApplies++; return; }\n      address += BigInt(delta * 8);\n      if (address < seg.address || address + ptrSize > seg.address + seg.size) { fail('threaded binding delta leaves segment'); return; }\n    }\n    fail('threaded binding chain exceeded the 100000-entry budget');\n  };\n  while (p < end) {\n    const byte = r.u8(p++);\n    const op = byte & BIND_OPCODE_MASK;\n    const imm = byte & BIND_IMMEDIATE_MASK;\n    if (op === 0x00) {\n      if (source === 'lazy-bind') { symbol = ''; symbolFlags = 0; libOrdinal = 0; addend = 0n; continue; }\n      break;\n    } else if (op === 0x10) libOrdinal = imm;\n    else if (op === 0x20) { const x = r.uleb(p); p = x.next; libOrdinal = Number(x.value); }\n    else if (op === 0x30) libOrdinal = imm === 0 ? 0 : signExtend(imm | 0xf0, 8);\n    else if (op === 0x40) { const x = rawCString(r, p, end); symbol = x.text; symbolFlags = imm; p = x.next; }\n    else if (op === 0x50) type = imm;\n    else if (op === 0x60) { const x = r.sleb(p); p = x.next; addend = x.value; }\n    else if (op === 0x70) { segIndex = imm; const x = r.uleb(p); p = x.next; segOffset = x.value; }\n    else if (op === 0x80) { const x = r.uleb(p); p = x.next; segOffset += x.value; }\n    else if (op === 0x90) { bind(); segOffset += ptrSize; }\n    else if (op === 0xa0) { bind(); const x = r.uleb(p); p = x.next; segOffset += ptrSize + x.value; }\n    else if (op === 0xb0) { bind(); segOffset += ptrSize + BigInt(imm) * ptrSize; }\n    else if (op === 0xc0) {\n      const a = r.uleb(p); p = a.next; const b = r.uleb(p); p = b.next;\n      if (a.value > 10_000_000n) { fail('bind repeat count exceeds budget'); break; }\n      for (let i = 0n; i < a.value; i++) { bind(); segOffset += ptrSize + b.value; }\n    } else if (op === 0xd0) {\n      if (imm === 0) {\n        const x = r.uleb(p); p = x.next;\n        if (x.value > 65536n) { fail('threaded ordinal table exceeds 65536 entries'); break; }\n        threadedTableLimit = Number(x.value); threadedTable = [];\n      } else if (imm === 1) applyThreaded();\n      else { fail(\`unknown threaded bind subopcode 0x${'${imm.toString(16)}'}\`, byte); break; }\n    } else { fail(\`unknown dyld bind opcode 0x${'${op.toString(16)}'}\`, byte); break; }\n  }\n  if (threadedTable && threadedTable.length !== threadedTableLimit) fail(\`threaded ordinal table expected ${'${threadedTableLimit}'} entries, decoded ${'${threadedTable.length}'}\`);\n  return status;\n}\n\nexport function parseExportTrie`);

// #68/#70/#71 — byte-accurate strings, cycle/budget safety, and correct export kinds.
replaceRe('js/binary/macho-dyld.js', /export function parseExportTrie\([\s\S]*?\n\}\n\nfunction dylibForOrdinal/, `export function parseExportTrie(r, dc, image) {\n  if (!dc || !dc.size || dc.offset + dc.size > r.length) return null;\n  const base = dc.offset, end = dc.offset + dc.size;\n  const active = new Set();\n  const status = { complete: true, nodes: 0, edges: 0, cycleDetected: false, budgetExceeded: false };\n  image.metadata.exportTrie = status;\n  const markPartial = (message, field) => { status.complete = false; if (field) status[field] = true; image.warnings.push(\`exports trie: ${'${message}'}\`); };\n  const walk = (nodeOff, prefix, depth) => {\n    if (depth > 256) { markPartial('depth budget exceeded', 'budgetExceeded'); return; }\n    if (!Number.isSafeInteger(nodeOff) || nodeOff < 0 || base + nodeOff >= end) { markPartial('child node offset is outside trie'); return; }\n    if (active.has(nodeOff)) { markPartial(\`cycle detected at node 0x${'${nodeOff.toString(16)}'}\`, 'cycleDetected'); return; }\n    if (++status.nodes > 1_000_000) { markPartial('node budget exceeded', 'budgetExceeded'); return; }\n    active.add(nodeOff);\n    try {\n      let p = base + nodeOff;\n      const term = r.uleb(p); p = term.next;\n      const terminalSize = Number(term.value);\n      if (!Number.isSafeInteger(terminalSize) || terminalSize < 0 || p + terminalSize > end) { markPartial('terminal payload is truncated'); return; }\n      const terminalEnd = p + terminalSize;\n      if (term.value) {\n        const flagsX = r.uleb(p); p = flagsX.next; const flags = Number(flagsX.value);\n        if (flags & 0x08) {\n          const ord = r.uleb(p); p = ord.next; const importedX = rawCString(r, p, terminalEnd);\n          image.exports.push({ name: prefix, address: 0n, kind: 'reexport', flags, ordinal: Number(ord.value), imported: importedX.text || null, source: 'exports-trie' });\n        } else {\n          const addrX = r.uleb(p); p = addrX.next; const exportKind = flags & 0x03;\n          const address = exportKind === 0 ? image.imageBase + addrX.value : addrX.value;\n          const kind = exportKind === 1 ? 'thread-local' : exportKind === 2 ? 'absolute' : 'export';\n          const ex = { name: prefix, address, kind, flags, source: 'exports-trie' };\n          if (flags & 0x10) { const resolverX = r.uleb(p); p = resolverX.next; ex.resolver = image.imageBase + resolverX.value; }\n          image.exports.push(ex);\n          if (exportKind === 0) { const sec = image.sectionAt(address); if (sec && sec.perms.execute) image.functions.push(functionSeed(address, { name: prefix, source: 'export', confidence: 0.9 })); }\n        }\n      }\n      p = terminalEnd; if (p >= end) return;\n      const children = r.u8(p++);\n      for (let i = 0; i < children; i++) {\n        if (++status.edges > 2_000_000) { markPartial('edge budget exceeded', 'budgetExceeded'); return; }\n        const edgeX = rawCString(r, p, end); const edge = edgeX.text; p = edgeX.next;\n        if (p >= end) { markPartial('child offset is truncated'); return; }\n        const child = r.uleb(p); p = child.next; walk(Number(child.value), prefix + edge, depth + 1);\n      }\n    } finally { active.delete(nodeOff); }\n  };\n  try { walk(0, '', 0); } catch (e) {\n    if (e?.code === 'BINARY_SOURCE_RANGE_MISSING') throw e; markPartial(e.message);\n  }\n  return status;\n}\n\nfunction rawCString(r, p, end) {\n  const start = p;\n  while (p < end && r.u8(p) !== 0) p++;\n  if (p >= end) throw new Error('unterminated C string');\n  const raw = r.slice(start, p - start);\n  let text;\n  try { text = new TextDecoder('utf-8', { fatal: false }).decode(raw); }\n  catch { text = Array.from(raw, (c) => c >= 0x20 && c <= 0x7e ? String.fromCharCode(c) : '\\uFFFD').join(''); }\n  return { text, next: p + 1, bytes: p + 1 - start };\n}\n\nfunction dylibForOrdinal`);

// Add focused regressions to the existing binary:test Mach-O suite.
replace('tests/universal-binary.mjs', `  ByteView, detectBinary, openBinary, auditBinary,`, `  BinaryImage, ByteView, detectBinary, openBinary, auditBinary,`);
replace('tests/universal-binary.mjs', `} from '../js/binary/index.js';`, `} from '../js/binary/index.js';\nimport { parseChainedImports, parseChainedBindingSites, parseClassicBindings, parseExportTrie } from '../js/binary/macho-dyld.js';`);
replace('tests/universal-binary.mjs', `function testMachO() {`, `function makeLegacyThreadMachOFixture() {\n  const b = new Uint8Array(0x400), v = new DataView(b.buffer);\n  const w16=(o,x)=>v.setUint16(o,x,true), w32=(o,x)=>v.setUint32(o,x,true), wi32=(o,x)=>v.setInt32(o,x,true), w64=(o,x)=>v.setBigUint64(o,BigInt(x),true);\n  w32(0,0xfeedfacf); wi32(4,0x0100000c); wi32(8,0); w32(12,2); w32(16,3); w32(20,72+288+16); w32(24,0); w32(28,0);\n  let p=32; w32(p,0x19); w32(p+4,72); for (const [i,c] of [...'__TEXT'].entries()) b[p+8+i]=c.charCodeAt(0); w64(p+24,0x100000000n); w64(p+32,0x1000n); w64(p+40,0); w64(p+48,0x400n); wi32(p+56,5); wi32(p+60,5); w32(p+64,0); w32(p+68,0);\n  p+=72; w32(p,0x5); w32(p+4,288); w32(p+8,6); w32(p+12,68); w64(p+16+264,0x100000180n);\n  p+=288; w32(p,0x25); w32(p+4,16); w32(p+8,0x00110002); w32(p+12,0x00120000);\n  return b;\n}\n\nfunction testMachOIssueRegressions() {\n  const legacy=openBinary(makeLegacyThreadMachOFixture());\n  assert.equal(legacy.entrypoint,0x100000180n); assert.equal(legacy.metadata.entrypointSource,'LC_UNIXTHREAD');\n  assert.equal(legacy.platform,'iOS'); assert.equal(legacy.metadata.buildVersion.minos,'17.0.2'); assert.equal(legacy.metadata.buildVersion.source,'LC_VERSION_MIN');\n\n  const siteImage=new BinaryImage(new Uint8Array(1),{format:'test'});\n  siteImage.imports.push({name:'x',library:'L',ordinal:1,sites:[\n    {address:1n,offset:0n,kind:'bind',type:1,addend:0n,pointerFormat:1},\n    {address:1n,offset:0n,kind:'bind',type:2,addend:0n,pointerFormat:1},\n    {address:1n,offset:0n,kind:'bind',type:1,addend:4n,pointerFormat:1},\n    {address:1n,offset:0n,kind:'bind',type:1,addend:0n,pointerFormat:2},\n  ]}); siteImage.finalize(); assert.equal(siteImage.imports[0].sites.length,4);\n  siteImage.metadata.negative=-1n; assert.equal(siteImage.toJSON().metadata.negative,'-0x1');\n\n  const bindBytes=new Uint8Array(0x200), bv=new DataView(bindBytes.buffer);\n  const segmentImage=new BinaryImage(bindBytes,{format:'macho',bits:64,imageBase:0x1000n});\n  const seg=segmentImage.addSegment({name:'DATA',address:0x1000n,size:0x100n,fileOffset:0n,fileSize:0x100n,perms:{read:true,write:true}}); segmentImage.libraries=['libX'];\n  let s=0x100; bindBytes.set([0x11,0x41,0x66,0x6f,0x6f,0x00,0x70,0x00,0x90,0x00],s);\n  parseClassicBindings(new ByteView(bindBytes),{offset:s,size:10},segmentImage,[seg],'bind');\n  assert.equal(segmentImage.imports[0].weak,true); assert.equal(segmentImage.imports[0].symbolFlags,1);\n\n  const invalidImage=new BinaryImage(bindBytes,{format:'macho',bits:64,imageBase:0x1000n}); const invalidSeg=invalidImage.addSegment({address:0x1000n,size:0x100n,fileOffset:0n,fileSize:0x100n,perms:{read:true,write:true}}); invalidImage.libraries=['libX'];\n  s=0x120; bindBytes.set([0x11,0x40,0x66,0x80,0x6f,0x00,0x70,0x00,0x90,0x00],s);\n  parseClassicBindings(new ByteView(bindBytes),{offset:s,size:10},invalidImage,[invalidSeg],'bind'); assert.equal(invalidImage.imports.length,1);\n  s=0x140; bindBytes.set([0x40,0x78,0x00,0x70,0x82,0x02,0x90,0x00],s);\n  const oobImage=new BinaryImage(bindBytes,{format:'macho',bits:64,imageBase:0x1000n}); const oobSeg=oobImage.addSegment({address:0x1000n,size:0x100n,fileOffset:0n,fileSize:0x100n,perms:{read:true,write:true}});\n  const oobStatus=parseClassicBindings(new ByteView(bindBytes),{offset:s,size:8},oobImage,[oobSeg],'bind'); assert.equal(oobImage.imports.length,0); assert.equal(oobStatus.complete,false);\n  s=0x150; bindBytes[s]=0xe0; const unknown=parseClassicBindings(new ByteView(bindBytes),{offset:s,size:1},oobImage,[oobSeg],'weak-bind'); assert.equal(unknown.complete,false); assert.ok(unknown.unsupportedOpcodes.length);\n\n  const threadedImage=new BinaryImage(bindBytes,{format:'macho',bits:64,imageBase:0x1000n}); const threadedSeg=threadedImage.addSegment({address:0x1000n,size:0x100n,fileOffset:0n,fileSize:0x100n,perms:{read:true,write:true}}); threadedImage.libraries=['libX'];\n  bv.setBigUint64(0x20,1n<<62n,true); s=0x170; bindBytes.set([0x11,0x40,0x74,0x00,0xd0,0x01,0x90,0x70,0x20,0xd1,0x00],s);\n  const threaded=parseClassicBindings(new ByteView(bindBytes),{offset:s,size:11},threadedImage,[threadedSeg],'bind'); assert.equal(threaded.complete,true); assert.equal(threadedImage.imports[0].sites[0].kind,'threaded-bind');\n\n  const cycleBytes=Uint8Array.of(0,1,0x61,0,0); const cycleImage=new BinaryImage(cycleBytes,{format:'macho'});\n  const cycle=parseExportTrie(new ByteView(cycleBytes),{offset:0,size:cycleBytes.length},cycleImage); assert.equal(cycle.complete,false); assert.equal(cycle.cycleDetected,true);\n  const absBytes=Uint8Array.of(0,1,0x61,0x62,0x73,0,7,2,2,5,0); const absImage=new BinaryImage(absBytes,{format:'macho',imageBase:0x100000000n});\n  parseExportTrie(new ByteView(absBytes),{offset:0,size:absBytes.length},absImage); assert.equal(absImage.exports[0].kind,'absolute'); assert.equal(absImage.exports[0].address,5n);\n\n  const chained=new Uint8Array(0x100), cv=new DataView(chained.buffer); cv.setUint32(4,28,true); cv.setUint32(28,1,true); cv.setUint32(32,8,true); cv.setUint32(36,24,true); cv.setUint16(40,0x1000,true); cv.setUint16(42,99,true); cv.setBigUint64(44,0x80n,true); cv.setUint16(56,1,true); cv.setUint16(58,0,true);\n  const chainImage=new BinaryImage(chained,{format:'macho',bits:64,imageBase:0x1000n}); chainImage.addSegment({address:0x1000n,size:0x200n,fileOffset:0n,fileSize:0x100n,perms:{read:true}}); chainImage.metadata.chainedFixups={};\n  parseChainedBindingSites(new ByteView(chained),{offset:0,size:0x80},chainImage,[]); assert.deepEqual(chainImage.metadata.chainedFixups.unsupportedPointerFormats,[99]); assert.equal(chainImage.metadata.chainedFixups.complete,false);\n\n  const compressed=new Uint8Array(64), cmpv=new DataView(compressed.buffer); cmpv.setUint32(16,1,true); cmpv.setUint32(20,1,true); cmpv.setUint32(24,1,true); const compressedImage=new BinaryImage(compressed,{format:'macho'});\n  assert.equal(parseChainedImports(new ByteView(compressed),{offset:0,size:64},compressedImage),null); assert.equal(compressedImage.metadata.chainedFixups.symbolsComplete,false);\n}\n\nfunction testMachO() {`);
replace('tests/universal-binary.mjs', `  testReader(); testDetection(); testFingerprint(); testMachO(); testElf(); testPe(); testRealMachO();`, `  testReader(); testDetection(); testFingerprint(); testMachO(); testMachOIssueRegressions(); testElf(); testPe(); testRealMachO();`);

fs.rmSync('tools/fix-issues-61-73.mjs');
fs.rmSync('.github/workflows/fix-issues-61-73.yml');
