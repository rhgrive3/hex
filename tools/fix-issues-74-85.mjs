import fs from 'node:fs';
function read(path){return fs.readFileSync(path,'utf8');}
function write(path,s){fs.writeFileSync(path,s);}
function replace(path,before,after){const s=read(path),n=s.split(before).length-1;if(n!==1)throw new Error(`${path}: expected one match, got ${n}\n${before}`);write(path,s.replace(before,after));}

// #74-#77 — validate ELF table contracts before any indexed reads, including PN_XNUM.
replace('js/binary/elf.js', `const PT_LOAD = 1;`, `const PT_LOAD = 1;\nconst PT_GNU_EH_FRAME = 0x6474e550;\nconst PN_XNUM = 0xffff;`);
replace('js/binary/elf.js', `const SHT_DYNSYM = 11;\nconst SHT_REL = 9;`, `const SHT_DYNSYM = 11;\nconst SHT_REL = 9;\nconst SHT_SYMTAB_SHNDX = 18;`);
replace('js/binary/elf.js', `const SHN_UNDEF = 0;`, `const SHN_UNDEF = 0;\nconst SHN_XINDEX = 0xffff;`);
replace('js/binary/elf.js', `  const h = readHeader(r, bits);\n  const image = new BinaryImage(bytes, {`, `  const h = readHeader(r, bits);\n  validateHeaderTableSizes(r, h, bits);\n  resolveExtendedProgramHeaderCount(r, h, bits);\n  const image = new BinaryImage(bytes, {`);
replace('js/binary/elf.js', `    metadata: { type: h.type, machine: h.machine, flags: h.flags, osabi: r.u8(7), abiVersion: r.u8(8) },`, `    metadata: { type: h.type, machine: h.machine, flags: h.flags, osabi: r.u8(7), abiVersion: r.u8(8), extendedProgramHeaderCount: h.extendedPhnum ?? null },`);
replace('js/binary/elf.js', `function parseProgramHeaders(r, h, image, bits) {`, `function validateHeaderTableSizes(r, h, bits) {\n  const minHeader = bits === 64 ? 64 : 52;\n  const minProgram = bits === 64 ? 56 : 32;\n  const minSection = bits === 64 ? 64 : 40;\n  if (h.ehsize < minHeader) throw new Error(\`ELF e_ehsize ${'${h.ehsize}'} is smaller than ${'${minHeader}'}\`);\n  if (h.phoff !== 0n && h.phnum !== 0 && h.phentsize < minProgram) throw new Error(\`ELF e_phentsize ${'${h.phentsize}'} is smaller than ${'${minProgram}'}\`);\n  if (h.shoff !== 0n && h.shentsize < minSection) throw new Error(\`ELF e_shentsize ${'${h.shentsize}'} is smaller than ${'${minSection}'}\`);\n  if (h.phnum === PN_XNUM && h.shoff === 0n) throw new Error('ELF PN_XNUM requires section header 0');\n  void r;\n}\n\nfunction resolveExtendedProgramHeaderCount(r, h, bits) {\n  if (h.phnum !== PN_XNUM) return;\n  const off = safeOffset(h.shoff);\n  const minSection = bits === 64 ? 64 : 40;\n  if (off == null || off <= 0 || off + minSection > r.length) throw new Error('ELF PN_XNUM section header 0 is truncated');\n  const actual = r.u32(off + (bits === 64 ? 44 : 28));\n  if (actual > 1_000_000) throw new Error(\`invalid ELF extended program header count ${'${actual}'}\`);\n  h.extendedPhnum = actual;\n  h.phnum = actual;\n}\n\nfunction parseProgramHeaders(r, h, image, bits) {`);

// #78/#80 — validate symbol entry size and resolve SHN_XINDEX via SHT_SYMTAB_SHNDX.
replace('js/binary/elf.js', `function parseSymbols(r, table, sections, image, bits) {\n  const str = sections[table.link];\n  if (!str || str.type !== SHT_STRTAB || !table.entsize) return;\n  const count = Number(table.size / table.entsize);`, `function parseSymbols(r, table, sections, image, bits) {\n  const str = sections[table.link];\n  if (!str || str.type !== SHT_STRTAB || !table.entsize) return;\n  const minEnt = BigInt(bits === 64 ? 24 : 16);\n  if (table.entsize < minEnt) { image.warnings.push(\`ELF symbol table ${'${table.index}'} entry size ${'${table.entsize}'} is smaller than ${'${minEnt}'}\`); return; }\n  const xindex = sections.find((s) => s.type === SHT_SYMTAB_SHNDX && s.link === table.index) || null;\n  if (xindex && (xindex.entsize && xindex.entsize < 4n || xindex.offset + xindex.size > BigInt(r.length))) { image.warnings.push(\`ELF SHT_SYMTAB_SHNDX for table ${'${table.index}'} is malformed\`); }\n  const count = Number(table.size / table.entsize);`);
replace('js/binary/elf.js', `    const bind = info >>> 4, type = info & 0xf;\n    const defined = shndx !== SHN_UNDEF;`, `    const bind = info >>> 4, type = info & 0xf;\n    let resolvedShndx = shndx;\n    if (shndx === SHN_XINDEX) {\n      const xoff = xindex ? safeOffset(xindex.offset + BigInt(i * 4)) : null;\n      if (xoff == null || !xindex || xoff + 4 > r.length || BigInt((i + 1) * 4) > xindex.size) {\n        image.warnings.push(\`ELF symbol ${'${i}'} uses SHN_XINDEX without a valid SHT_SYMTAB_SHNDX entry\`);\n        resolvedShndx = null;\n      } else resolvedShndx = r.u32(xoff);\n    }\n    const defined = resolvedShndx == null ? true : resolvedShndx !== SHN_UNDEF;`);
replace('js/binary/elf.js', `sectionIndex: shndx, visibility: other & 3`, `sectionIndex: resolvedShndx ?? shndx, visibility: other & 3`);

// #79 — REL/RELA strides are ABI-defined minimums; reject undersized table entries.
replace('js/binary/elf.js', `function parseRelocations(r, sec, sections, image, bits) {\n  if (!sec.entsize) return;`, `function parseRelocations(r, sec, sections, image, bits) {\n  if (!sec.entsize) return;\n  const minEnt = BigInt(bits === 64 ? (sec.type === SHT_RELA ? 24 : 16) : (sec.type === SHT_RELA ? 12 : 8));\n  if (sec.entsize < minEnt) { image.warnings.push(\`ELF relocation section ${'${sec.index}'} entry size ${'${sec.entsize}'} is smaller than ${'${minEnt}'}\`); return; }`);

// #81 — PT_DYNAMIC augments section metadata item-by-item instead of being all-or-nothing fallback.
replace('js/binary/elf.js', `  const hasDynamic = rawSections.some((s) => s.type === SHT_DYNAMIC);\n  if (!hasDynsym || !hasRelocations || !hasDynamic) {\n    parseProgramDynamic(r, programHeaders, image, bits, {\n      symbols: !hasDynsym,\n      relocations: !hasRelocations,\n    });\n  }`, `  const hasDynamic = rawSections.some((s) => s.type === SHT_DYNAMIC);\n  parseProgramDynamic(r, programHeaders, image, bits, {\n    symbols: !hasDynsym,\n    relocations: !hasRelocations,\n    sectionDynamicPresent: hasDynamic,\n  });`);

// #82 — discover .eh_frame_hdr through PT_GNU_EH_FRAME when section headers are absent/incomplete.
replace('js/binary/elf.js', `  const ehFrameHdr = rawSections.find((s) => s.name === '.eh_frame_hdr');\n  if (ehFrameHdr) parseEhFrameHeader(r, ehFrameHdr, image, bits);`, `  let ehFrameHdr = rawSections.find((s) => s.name === '.eh_frame_hdr') || null;\n  if (!ehFrameHdr) {\n    const ph = programHeaders.find((item) => item.type === PT_GNU_EH_FRAME && item.filesz > 0n);\n    if (ph) ehFrameHdr = { name: 'PT_GNU_EH_FRAME', addr: ph.vaddr, offset: ph.offset, size: ph.filesz };\n  }\n  if (ehFrameHdr) parseEhFrameHeader(r, ehFrameHdr, image, bits);`);

// #83-#85 — validate dynamic entry sizes and never guess an unknown PLT relocation encoding.
replace('js/binary/elf-dynamic.js', `  const syment = one(DT_SYMENT) || BigInt(bits === 64 ? 24 : 16);`, `  const defaultSyment = BigInt(bits === 64 ? 24 : 16);\n  const syment = one(DT_SYMENT) ?? defaultSyment;\n  const symentValid = syment >= defaultSyment;\n  if (!symentValid) markDynamicPartial(image, \`DT_SYMENT ${'${syment}'} is smaller than ${'${defaultSyment}'}\`);`);
replace('js/binary/elf-dynamic.js', `  if (symtab != null && syment > 0n) {`, `  if (symtab != null && symentValid) {`);
replace('js/binary/elf-dynamic.js', `  const addTable = (va, size, ent, rela, source) => {\n    if (va == null || size == null || size <= 0n) return;\n    const off = vaToOffset(image, va);\n    const n = toSafeNumber(size);\n    const e = toSafeNumber(ent || BigInt(bits === 64 ? (rela ? 24 : 16) : (rela ? 12 : 8)));`, `  const addTable = (va, size, ent, rela, source) => {\n    if (va == null || size == null || size <= 0n) return;\n    const off = vaToOffset(image, va);\n    const n = toSafeNumber(size);\n    const minimum = BigInt(bits === 64 ? (rela ? 24 : 16) : (rela ? 12 : 8));\n    const requested = ent ?? minimum;\n    if (requested < minimum) { markDynamicPartial(image, \`${'${source}'} entry size ${'${requested}'} is smaller than ${'${minimum}'}\`); return; }\n    const e = toSafeNumber(requested);`);
replace('js/binary/elf-dynamic.js', `  if (jmprel != null && pltsz != null) {\n    const rela = pltrel === DT_RELA;\n    addTable(jmprel, pltsz, rela ? one(DT_RELAENT) : one(DT_RELENT), rela, rela ? 'PT_DYNAMIC-JMPREL-RELA' : 'PT_DYNAMIC-JMPREL-REL');\n  }`, `  if (jmprel != null && pltsz != null) {\n    if (pltrel === DT_RELA) addTable(jmprel, pltsz, one(DT_RELAENT), true, 'PT_DYNAMIC-JMPREL-RELA');\n    else if (pltrel === DT_REL) addTable(jmprel, pltsz, one(DT_RELENT), false, 'PT_DYNAMIC-JMPREL-REL');\n    else markDynamicPartial(image, \`DT_PLTREL has unsupported value ${'${pltrel == null ? \'<missing>\' : pltrel}'}; JMPREL was not decoded\`);\n  }`);
replace('js/binary/elf-dynamic.js', `function vaToOffset(image, va) {`, `function markDynamicPartial(image, message) {\n  image.metadata.programDynamicPartial = true;\n  const diagnostics = image.metadata.programDynamicDiagnostics ||= [];\n  if (!diagnostics.includes(message)) diagnostics.push(message);\n  image.warnings.push(\`PT_DYNAMIC: ${'${message}'}\`);\n}\n\nfunction vaToOffset(image, va) {`);

// Permanent regressions live in the already-invoked sectionless ELF suite.
replace('tests/universal-binary-sectionless.mjs', `import { openBinary, auditBinary, fingerprintImage } from '../js/binary/index.js';`, `import { openBinary, auditBinary, fingerprintImage } from '../js/binary/index.js';\nimport { makeElf64Fixture } from './universal-binary.mjs';`);
replace('tests/universal-binary-sectionless.mjs', `if (import.meta.url === pathToFileURL(process.argv[1]).href) {`, `function issue74To85Regressions() {\n  const malformed = (mutate, pattern) => { const b=makeElf64Fixture(); mutate(new DataView(b.buffer),b); assert.throws(()=>openBinary(b),pattern); };\n  malformed((v)=>v.setUint16(52,63,true),/e_ehsize/);\n  const withPh=makeElf64Fixture(); const wpv=new DataView(withPh.buffer); wpv.setBigUint64(32,0x40n,true); wpv.setUint16(56,1,true); wpv.setUint16(54,55,true); assert.throws(()=>openBinary(withPh),/e_phentsize/);\n  malformed((v)=>v.setUint16(58,63,true),/e_shentsize/);\n\n  const xnum=makeElf64Fixture(), xv=new DataView(xnum.buffer); xv.setBigUint64(32,0x40n,true); xv.setUint16(56,0xffff,true); xv.setUint32(0x280+44,1,true); xv.setUint32(0x40,1,true); xv.setUint32(0x44,5,true); xv.setBigUint64(0x48,0n,true); xv.setBigUint64(0x50,0x400000n,true); xv.setBigUint64(0x60,BigInt(xnum.length),true); xv.setBigUint64(0x68,BigInt(xnum.length),true); xv.setBigUint64(0x70,0x1000n,true);\n  const xnumImage=openBinary(xnum); assert.equal(xnumImage.metadata.extendedProgramHeaderCount,1); assert.ok(xnumImage.segments.length>=1);\n\n  const shortSym=makeElf64Fixture(), ssv=new DataView(shortSym.buffer); ssv.setBigUint64(0x280+3*64+56,8n,true); const shortSymImage=openBinary(shortSym); assert.ok(shortSymImage.warnings.some(x=>x.includes('symbol table')&&x.includes('entry size')));\n  const shortRel=makeElf64Fixture(), srv=new DataView(shortRel.buffer), relSec=0x280+4*64; srv.setUint32(relSec+4,4,true); srv.setBigUint64(relSec+56,8n,true); srv.setUint32(relSec+40,3,true); const shortRelImage=openBinary(shortRel); assert.ok(shortRelImage.warnings.some(x=>x.includes('relocation section')&&x.includes('entry size')));\n\n  const xidx=makeElf64Fixture(), xiv=new DataView(xidx.buffer); xiv.setUint16(60,7,true); const xp=0x280+6*64; xiv.setUint32(xp+4,18,true); xiv.setBigUint64(xp+24,0x440n,true); xiv.setBigUint64(xp+32,12n,true); xiv.setUint32(xp+40,3,true); xiv.setBigUint64(xp+56,4n,true); xiv.setUint16(0x190+6,0xffff,true); xiv.setUint32(0x440+8,1,true); const xi=openBinary(xidx); assert.equal(xi.symbols.find(s=>s.name==='myfunc')?.sectionIndex,1);\n\n  const merged=makeElf64Fixture(), mv=new DataView(merged.buffer); mv.setBigUint64(32,0x40n,true); mv.setUint16(56,1,true); mv.setUint32(0x40,2,true); mv.setUint32(0x44,6,true); mv.setBigUint64(0x48,0x1c0n,true); mv.setBigUint64(0x50,0x404000n,true); mv.setBigUint64(0x60,32n,true); mv.setBigUint64(0x68,32n,true); mv.setBigUint64(0x70,8n,true); mv.setUint16(60,7,true); const mp=0x280+6*64; mv.setUint32(mp+4,4,true); mv.setBigUint64(mp+32,0n,true); mv.setUint32(mp+40,3,true); mv.setBigUint64(mp+56,24n,true); const mergedImage=openBinary(merged); assert.ok(mergedImage.metadata.programDynamic);\n\n  const eh=makeElf64Fixture(), ev=new DataView(eh.buffer); ev.setBigUint64(32,0x40n,true); ev.setUint16(56,1,true); ev.setUint32(0x40,0x6474e550,true); ev.setUint32(0x44,4,true); ev.setBigUint64(0x48,0x200n,true); ev.setBigUint64(0x50,0x405000n,true); ev.setBigUint64(0x60,16n,true); ev.setBigUint64(0x68,16n,true); ev.setBigUint64(0x70,4n,true); eh.set([1,0xff,3,3],0x200); ev.setUint32(0x204,1,true); ev.setUint32(0x208,0x401000,true); ev.setUint32(0x20c,0,true); const ehImage=openBinary(eh); assert.equal(ehImage.metadata.ehFrameHeader?.declaredFunctions,1); assert.ok(ehImage.functions.find(f=>f.address===0x401000n)?.sources?.includes('unwind'));\n\n  const syment=makeSectionlessElf64Fixture(), syv=new DataView(syment.buffer); syv.setBigUint64(0x200+4*16+8,8n,true); const sy=openBinary(syment); assert.ok(sy.warnings.some(x=>x.includes('DT_SYMENT')));\n  const relent=makeSectionlessElf64Fixture(), rv=new DataView(relent.buffer); rv.setBigUint64(0x200+8*16+8,8n,true); const ri=openBinary(relent); assert.ok(ri.warnings.some(x=>x.includes('entry size'))); assert.equal(ri.relocations.length,0);\n  const plt=makeSectionlessElf64Fixture(), pv=new DataView(plt.buffer); pv.setBigUint64(64+56+32,0xd0n,true); pv.setBigUint64(64+56+40,0xd0n,true); const dyn=(i,tag,val)=>{const p=0x200+i*16;pv.setBigInt64(p,BigInt(tag),true);pv.setBigUint64(p+8,BigInt(val),true);}; dyn(9,23,0x4003a0); dyn(10,2,24); dyn(11,20,999); dyn(12,0,0); const pi=openBinary(plt); assert.ok(pi.warnings.some(x=>x.includes('DT_PLTREL'))); assert.equal(pi.metadata.programDynamicPartial,true);\n}\n\nif (import.meta.url === pathToFileURL(process.argv[1]).href) {`);
replace('tests/universal-binary-sectionless.mjs', `  assert.ok(fingerprintImage(image).bytes>0);\n  console.log('universal-binary-sectionless: PASS');`, `  assert.ok(fingerprintImage(image).bytes>0);\n  issue74To85Regressions();\n  console.log('universal-binary-sectionless: PASS');`);

fs.rmSync('tools/fix-issues-74-85.mjs');
fs.rmSync('.github/workflows/fix-issues-74-85.yml');
