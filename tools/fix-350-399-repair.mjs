import fs from 'node:fs';
const path='tools/fix-350-399.mjs';
let s=fs.readFileSync(path,'utf8');
const start=s.indexOf("  s = replaceOnce(s,\n`  if (d.sub === 'inc')");
const marker="'#350 conditional aliases');";
const mark=s.indexOf(marker,start);
if(start<0||mark<0) throw new Error('obsolete #350 patch block not found');
const end=mark+marker.length;
s=s.slice(0,start)+s.slice(end);
fs.writeFileSync(path,s);
