import fs from 'node:fs';
function read(path){return fs.readFileSync(path,'utf8');}
function write(path,s){fs.writeFileSync(path,s);}
function replace(path,before,after){const s=read(path),n=s.split(before).length-1;if(n!==1)throw new Error(`${path}: expected one match, got ${n}\n${before}`);write(path,s.replace(before,after));}

// #86-#88 live in a dedicated decoder to keep the core PT_DYNAMIC parser bounded and reviewable.
write('js/binary/elf-extended.js', `const DT_RELRSZ = 35n;\nconst DT_RELR = 36n;\nconst DT_RELRENT = 37n;\nconst DT_ANDROID_REL = 0x6000000fn;\nconst DT_ANDROID_RELSZ = 0x60000010n;\nconst DT_ANDROID_RELA = 0x60000011n;\nconst DT_ANDROID_RELASZ = 0x60000012n;\nconst DT_VERSYM = 0x6ffffff0n;\nconst DT_VERDEF = 0x6ffffffcn;\nconst DT_VERDEFNUM = 0x6ffffffdn;\nconst DT_VERNEED = 0x6ffffffen;\nconst DT_VERNEEDNUM = 0x6fffffffn;\n\nconst GROUPED_BY_INFO = 1n;\nconst GROUPED_BY_OFFSET_DELTA = 2n;\nconst GROUPED_BY_ADDEND = 4n;\nconst GROUP_HAS_ADDEND = 8n;\n\nfunction one(tags, tag) { return tags.get(tag)?.[0] ?? null; }\nfunction safe(v) { const n=Number(v); return Number.isSafeInteger(n) && n >= 0 ? n : null; }\nfunction vaToOffset(image, va) { const off=image.addressToOffset(va); return off == null ? null : safe(off); }\nfunction partial(image, message) {\n  image.metadata.programDynamicPartial = true;\n  const list=image.metadata.programDynamicDiagnostics ||= [];\n  if (!list.includes(message)) list.push(message);\n  image.warnings.push(\`PT_DYNAMIC: ${'${message}'}\`);\n}\n\nexport function collectRelrRelocations(r, tags, image, bits) {\n  const va=one(tags,DT_RELR), size64=one(tags,DT_RELRSZ);\n  if (va == null || size64 == null || size64 === 0n) return [];\n  const word=bits===64?8:4, ent=one(tags,DT_RELRENT) ?? BigInt(word);\n  if (ent !== BigInt(word)) { partial(image,\`DT_RELRENT ${'${ent}'} does not match pointer size ${'${word}'}\`); return []; }\n  const off=vaToOffset(image,va), size=safe(size64);\n  if (off==null||size==null||off+size>r.length) { partial(image,'DT_RELR table is outside the file'); return []; }\n  if (size % word) partial(image,'DT_RELRSZ is not a multiple of DT_RELRENT');\n  const out=[]; let base=0n; const wordBits=BigInt(word*8);\n  const count=Math.min(Math.floor(size/word),10_000_000);\n  for(let i=0;i<count;i++){\n    const entry=word===8?r.u64(off+i*word):BigInt(r.u32(off+i*word));\n    if((entry&1n)===0n){ out.push({address:entry,symIndex:0,type:null,addend:null,source:'PT_DYNAMIC-RELR',relative:true}); base=entry+BigInt(word); continue; }\n    for(let bit=1n;bit<wordBits;bit++) if(entry&(1n<<bit)) out.push({address:base+(bit-1n)*BigInt(word),symIndex:0,type:null,addend:null,source:'PT_DYNAMIC-RELR',relative:true});\n    base+=(wordBits-1n)*BigInt(word);\n  }\n  return out;\n}\n\nfunction readSleb(r, state, end) {\n  let value=0n, shift=0n, byte=0;\n  for(let i=0;i<10;i++){\n    if(state.p>=end) throw new Error('truncated SLEB128');\n    byte=r.u8(state.p++); value|=BigInt(byte&0x7f)<<shift; shift+=7n;\n    if(!(byte&0x80)){ if((byte&0x40)&&shift<64n)value|=(-1n)<<shift; return value; }\n  }\n  throw new Error('SLEB128 exceeds 10 bytes');\n}\n\nfunction decodeAndroidTable(r, va, size64, image, bits, rela, source) {\n  const off=vaToOffset(image,va), size=safe(size64); if(off==null||size==null||off+size>r.length){partial(image,\`${'${source}'} table is outside the file\`);return [];}\n  const end=off+size; if(size<4||r.u8(off)!==0x41||r.u8(off+1)!==0x50||r.u8(off+2)!==0x53||r.u8(off+3)!==0x32){partial(image,\`${'${source}'} is not APS2 encoded\`);return [];}\n  const st={p:off+4}, out=[];\n  try {\n    const relocationCount=readSleb(r,st,end); if(relocationCount<0n||relocationCount>10_000_000n) throw new Error('relocation count exceeds budget');\n    let relocationOffset=readSleb(r,st,end), relocationAddend=0n, decoded=0n;\n    while(decoded<relocationCount){\n      const groupSize=readSleb(r,st,end), flags=readSleb(r,st,end); if(groupSize<=0n||groupSize>relocationCount-decoded) throw new Error('invalid relocation group size');\n      const groupedDelta=!!(flags&GROUPED_BY_OFFSET_DELTA), groupedInfo=!!(flags&GROUPED_BY_INFO), hasAddend=!!(flags&GROUP_HAS_ADDEND), groupedAddend=!!(flags&GROUPED_BY_ADDEND);\n      const groupDelta=groupedDelta?readSleb(r,st,end):0n, groupInfo=groupedInfo?readSleb(r,st,end):0n, groupAddend=hasAddend&&groupedAddend?readSleb(r,st,end):0n;\n      for(let i=0n;i<groupSize;i++,decoded++){\n        relocationOffset+=groupedDelta?groupDelta:readSleb(r,st,end);\n        const info=groupedInfo?groupInfo:readSleb(r,st,end);\n        if(hasAddend) relocationAddend+=groupedAddend?groupAddend:readSleb(r,st,end); else if(rela) relocationAddend=0n;\n        if(relocationOffset<0n||info<0n) throw new Error('negative relocation field');\n        const symIndex=bits===64?Number(info>>32n):Number(info>>8n), type=bits===64?Number(info&0xffffffffn):Number(info&0xffn);\n        if(!Number.isSafeInteger(symIndex)||!Number.isSafeInteger(type)) throw new Error('relocation info exceeds safe integer range');\n        out.push({address:relocationOffset,symIndex,type,addend:rela?relocationAddend:null,source});\n      }\n    }\n  } catch(error){partial(image,\`${'${source}'}: ${'${error.message}'}\`);}\n  return out;\n}\n\nexport function collectAndroidPackedRelocations(r,tags,image,bits){\n  const out=[]; const rel=one(tags,DT_ANDROID_REL), relsz=one(tags,DT_ANDROID_RELSZ), rela=one(tags,DT_ANDROID_RELA), relasz=one(tags,DT_ANDROID_RELASZ);\n  if(rel!=null&&relsz!=null) out.push(...decodeAndroidTable(r,rel,relsz,image,bits,false,'PT_DYNAMIC-ANDROID-REL'));\n  if(rela!=null&&relasz!=null) out.push(...decodeAndroidTable(r,rela,relasz,image,bits,true,'PT_DYNAMIC-ANDROID-RELA'));\n  return out;\n}\n\nexport function parseDynamicSymbolVersions(r,tags,image,symbolCount,stringAt){\n  const out=new Map(), versym=one(tags,DT_VERSYM); if(versym==null||symbolCount<=0)return out;\n  const voff=vaToOffset(image,versym); if(voff==null||voff+symbolCount*2>r.length){partial(image,'DT_VERSYM table is truncated');return out;}\n  const names=new Map();\n  const verdef=one(tags,DT_VERDEF), verdefnum=safe(one(tags,DT_VERDEFNUM)??0n);\n  if(verdef!=null&&verdefnum){let p=vaToOffset(image,verdef);for(let i=0;p!=null&&i<Math.min(verdefnum,65536);i++){if(p+20>r.length){partial(image,'DT_VERDEF is truncated');break;}const ndx=r.u16(p+4)&0x7fff,aux=r.u32(p+12),next=r.u32(p+16),ap=p+aux;if(ap+8>r.length){partial(image,'DT_VERDEF auxiliary entry is truncated');break;}const name=stringAt(BigInt(r.u32(ap)));if(name)names.set(ndx,{name,definition:true,library:null});if(!next)break;p+=next;}}\n  const verneed=one(tags,DT_VERNEED), verneednum=safe(one(tags,DT_VERNEEDNUM)??0n);\n  if(verneed!=null&&verneednum){let p=vaToOffset(image,verneed);for(let i=0;p!=null&&i<Math.min(verneednum,65536);i++){if(p+16>r.length){partial(image,'DT_VERNEED is truncated');break;}const cnt=r.u16(p+2),file=stringAt(BigInt(r.u32(p+4))),aux=r.u32(p+8),next=r.u32(p+12);let ap=p+aux;for(let j=0;j<cnt&&j<65536;j++){if(ap+16>r.length){partial(image,'DT_VERNEED auxiliary entry is truncated');break;}const other=r.u16(ap+6)&0x7fff,name=stringAt(BigInt(r.u32(ap+8))),anext=r.u32(ap+12);if(name)names.set(other,{name,definition:false,library:file||null});if(!anext)break;ap+=anext;}if(!next)break;p+=next;}}\n  for(let i=0;i<symbolCount;i++){const raw=r.u16(voff+i*2),index=raw&0x7fff;if(index<=1)continue;const named=names.get(index);out.set(i,{index,hidden:!!(raw&0x8000),name:named?.name||null,library:named?.library||null,definition:named?.definition??null});}\n  image.metadata.symbolVersions={entries:out.size,named:[...out.values()].filter(v=>v.name).length};\n  return out;\n}\n`);

replace('js/binary/elf-dynamic.js', `import { functionSeed } from './model.js';`, `import { functionSeed } from './model.js';\nimport { collectAndroidPackedRelocations, collectRelrRelocations, parseDynamicSymbolVersions } from './elf-extended.js';`);

// #86/#87 — merge standard RELR and Android APS2 relocations into the normal relocation pipeline.
replace('js/binary/elf-dynamic.js', `  const relocs = collectDynamicRelocations(r, tags, image, bits);\n  let symbolCount = 0;`, `  const relocs = collectDynamicRelocations(r, tags, image, bits);\n  relocs.push(...collectRelrRelocations(r, tags, image, bits));\n  relocs.push(...collectAndroidPackedRelocations(r, tags, image, bits));\n  let symbolCount = 0;\n  let symbolCountSource = 'none';`);

// #89 — source the count from hashes first; a layout estimate is explicitly partial and tightly bounded.
replace('js/binary/elf-dynamic.js', `  if (symtab != null && syment > 0n) {\n    symbolCount = symbolCountFromHash(r, one(DT_HASH), image) ||\n      symbolCountFromGnuHash(r, one(DT_GNU_HASH), image, bits) ||\n      symbolCountFromRelocations(relocs) ||\n      symbolCountFromLayout(symtab, strtab, syment);\n  }`, `  if (symtab != null && syment > 0n) {\n    symbolCount = symbolCountFromHash(r, one(DT_HASH), image);\n    if (symbolCount) symbolCountSource = 'sysv-hash';\n    if (!symbolCount) { symbolCount = symbolCountFromGnuHash(r, one(DT_GNU_HASH), image, bits); if (symbolCount) symbolCountSource = 'gnu-hash'; }\n    if (!symbolCount) { symbolCount = symbolCountFromRelocations(relocs); if (symbolCount) symbolCountSource = 'relocations'; }\n    if (!symbolCount) {\n      symbolCount = symbolCountFromLayout(symtab, strtab, syment, image, r);\n      if (symbolCount) { symbolCountSource = 'layout-heuristic'; markExtendedPartial(image, 'dynamic symbol count was inferred from bounded SYMTAB/STRTAB layout'); }\n    }\n  }`);

// #88 — parse GNU/Solaris version tables before symbol materialization and preserve version identity.
replace('js/binary/elf-dynamic.js', `  let symbols = [];\n  if (opts.symbols !== false && symtab != null && symbolCount > 0) {\n    symbols = parseDynamicSymbols(r, image, bits, symtab, syment, symbolCount, stringAt);`, `  const versions = parseDynamicSymbolVersions(r, tags, image, symbolCount, stringAt);\n  let symbols = [];\n  if (opts.symbols !== false && symtab != null && symbolCount > 0) {\n    symbols = parseDynamicSymbols(r, image, bits, symtab, syment, symbolCount, stringAt, versions);`);
replace('js/binary/elf-dynamic.js', `  if (opts.relocations !== false) attachDynamicRelocations(image, relocs, symbols);`, `  applyVersionMetadata(image, versions);\n  if (opts.relocations !== false) attachDynamicRelocations(image, relocs, symbols);`);
replace('js/binary/elf-dynamic.js', `    symbols: symbolCount,`, `    symbols: symbolCount,\n    symbolCountSource,`);
replace('js/binary/elf-dynamic.js', `function parseDynamicSymbols(r, image, bits, symtabVa, syment, count, stringAt) {`, `function parseDynamicSymbols(r, image, bits, symtabVa, syment, count, stringAt, versions = new Map()) {`);
replace('js/binary/elf-dynamic.js', `    const sym = { name, address: value, size, kind, binding, defined, sectionIndex: shndx, visibility: other & 3, source: 'PT_DYNAMIC', index: i, tableIndex: -1 };`, `    const ver = versions.get(i) || null;\n    const sym = { name, address: value, size, kind, binding, defined, sectionIndex: shndx, visibility: other & 3, source: 'PT_DYNAMIC', index: i, tableIndex: -1, versionIndex: ver?.index ?? null, version: ver?.name ?? null, versionHidden: ver?.hidden ?? false, versionLibrary: ver?.library ?? null };`);
replace('js/binary/elf-dynamic.js', `    if (!defined && (bind === 1 || bind === 2)) image.imports.push({ name, library: null, ordinal: null, weak: bind === 2, source: 'PT_DYNAMIC', sites: [] });\n    if (defined && (bind === 1 || bind === 2) && sym.visibility !== 2) image.exports.push({ name, address: value, kind, source: 'PT_DYNAMIC' });`, `    if (!defined && (bind === 1 || bind === 2)) image.imports.push({ name, library: null, ordinal: null, weak: bind === 2, version: ver?.name ?? null, versionLibrary: ver?.library ?? null, versionIndex: ver?.index ?? null, source: 'PT_DYNAMIC', sites: [] });\n    if (defined && (bind === 1 || bind === 2) && (sym.visibility === 0 || sym.visibility === 3)) image.exports.push({ name, address: value, kind, version: ver?.name ?? null, versionIndex: ver?.index ?? null, source: 'PT_DYNAMIC' });`);
replace('js/binary/elf-dynamic.js', `function collectDynamicRelocations(r, tags, image, bits) {`, `function applyVersionMetadata(image, versions) {\n  if (!versions?.size) return;\n  for (const sym of image.symbols) {\n    if (sym.source !== 'dynsym' && sym.source !== 'PT_DYNAMIC') continue;\n    const ver = versions.get(sym.index); if (!ver) continue;\n    sym.versionIndex = ver.index; sym.version = ver.name; sym.versionHidden = ver.hidden; sym.versionLibrary = ver.library;\n    if (!sym.defined && sym.name) {\n      for (const imp of image.imports) if (imp.name === sym.name && imp.version == null) { imp.version = ver.name; imp.versionLibrary = ver.library; imp.versionIndex = ver.index; }\n    } else if (sym.defined && sym.name) {\n      for (const ex of image.exports) if (ex.name === sym.name && ex.address === sym.address && ex.version == null) { ex.version = ver.name; ex.versionIndex = ver.index; }\n    }\n  }\n}\n\nfunction collectDynamicRelocations(r, tags, image, bits) {`);

// #90 — GNU hash traversal has one global adversarial-input budget, not a budget per bucket.
replace('js/binary/elf-dynamic.js', `  let max = symOffset;\n  for (let i = 0; i < nbuckets; i++) {`, `  let max = symOffset;\n  let remainingSteps = Math.min(10_000_000, Math.max(4096, nbuckets * 64));\n  for (let i = 0; i < nbuckets; i++) {`);
replace('js/binary/elf-dynamic.js', `    for (let guard = 0; guard < 10_000_000 && p + 4 <= r.length; guard++, idx++, p += 4) {\n      const chain = r.u32(p);`, `    for (; p + 4 <= r.length; idx++, p += 4) {\n      if (--remainingSteps < 0) { markExtendedPartial(image, 'GNU hash chain traversal exceeded the global budget'); return 0; }\n      const chain = r.u32(p);`);

replace('js/binary/elf-dynamic.js', `function symbolCountFromLayout(symtab, strtab, syment) {\n  if (strtab == null || strtab <= symtab || syment <= 0n) return 0;\n  const n = (strtab - symtab) / syment;\n  return n > 0n && n <= 10_000_000n ? Number(n) : 0;\n}`, `function symbolCountFromLayout(symtab, strtab, syment, image, r) {\n  if (strtab == null || strtab <= symtab || syment <= 0n) return 0;\n  const delta = strtab - symtab;\n  if (delta % syment !== 0n) return 0;\n  const n = delta / syment;\n  if (n <= 0n || n > 1_000_000n) return 0;\n  const symOff = vaToOffset(image, symtab), strOff = vaToOffset(image, strtab);\n  if (symOff == null || strOff == null || strOff <= symOff || strOff > r.length) return 0;\n  if (BigInt(strOff - symOff) !== delta) return 0;\n  return Number(n);\n}\n\nfunction markExtendedPartial(image, message) {\n  image.metadata.programDynamicPartial = true;\n  const list = image.metadata.programDynamicDiagnostics ||= [];\n  if (!list.includes(message)) list.push(message);\n  image.warnings.push(\`PT_DYNAMIC: ${'${message}'}\`);\n}`);

// #91 — STV_INTERNAL and STV_HIDDEN are both non-exportable.
replace('js/binary/elf.js', `    if (defined && (bind === 1 || bind === 2) && sym.visibility !== 2) image.exports.push({ name, address: value, kind, source: sym.source });`, `    if (defined && (bind === 1 || bind === 2) && (sym.visibility === 0 || sym.visibility === 3)) image.exports.push({ name, address: value, kind, source: sym.source });`);

// #92/#93 — funcrel needs a function base; indirect decoding must fail closed when dereference is impossible.
replace('js/binary/elf-unwind.js', `    image,\n    bits,`, `    image,\n    bits,\n    functionBase: null,`);
replace('js/binary/elf-unwind.js', `  else if (application === 0x40) { }`, `  else if (application === 0x40) {\n    if (ctx.functionBase == null) throw new Error('DW_EH_PE_funcrel requires a function base');\n    value += ctx.functionBase;\n  }`);
replace('js/binary/elf-unwind.js', `  if (indirect) {\n    const off = ctx.image.addressToOffset(value);\n    if (off != null && off + BigInt(ptrBytes) <= BigInt(r.length)) value = ctx.bits === 64 ? r.u64(Number(off)) : BigInt(r.u32(Number(off)));\n  }`, `  if (indirect) {\n    const off = ctx.image.addressToOffset(value);\n    if (off == null || off + BigInt(ptrBytes) > BigInt(r.length)) throw new Error(\`DW_EH_PE_indirect target 0x${'${value.toString(16)}'} is not readable\`);\n    value = ctx.bits === 64 ? r.u64(Number(off)) : BigInt(r.u32(Number(off)));\n  }`);

// #94/#97 — reject undersized optional headers and represent AddressOfEntryPoint==0 as null.
replace('js/binary/pe.js', `  const magic = r.u16(opt);\n  if (magic !== 0x10b && magic !== 0x20b) throw new Error(\`unsupported PE optional magic 0x${'${magic.toString(16)}'}\`);\n  const bits = magic === 0x20b ? 64 : 32;`, `  if (sizeOptional < 2) throw new Error('PE optional header is too small for its magic');\n  const magic = r.u16(opt);\n  if (magic !== 0x10b && magic !== 0x20b) throw new Error(\`unsupported PE optional magic 0x${'${magic.toString(16)}'}\`);\n  const bits = magic === 0x20b ? 64 : 32;\n  const minimumOptionalSize = bits === 64 ? 112 : 96;\n  if (sizeOptional < minimumOptionalSize) throw new Error(\`PE optional header size ${'${sizeOptional}'} is smaller than ${'${minimumOptionalSize}'}\`);`);
replace('js/binary/pe.js', `    imageBase, entrypoint: imageBase + BigInt(entryRva),`, `    imageBase, entrypoint: entryRva ? imageBase + BigInt(entryRva) : null,`);

// #95/#96 — virtual extent and file-backed extent are separate; raw alignment padding is not mapped memory.
replace('js/binary/pe.js', `    const size = BigInt(Math.max(virtualSize, sizeRaw));\n    const perms = { read: !!(flags & 0x40000000), write: !!(flags & 0x80000000), execute: !!(flags & 0x20000000) };\n    image.addSegment({ name, address, size, fileOffset: BigInt(ptrRaw), fileSize: BigInt(Math.min(sizeRaw, Math.max(0, bytes.length - ptrRaw))), perms, flags, source: 'PE-section' });\n    image.addSection({ name, address, size: BigInt(virtualSize || sizeRaw), fileOffset: BigInt(ptrRaw), fileSize: BigInt(sizeRaw), perms, flags, type: null, index: i + 1, source: 'PE-section' });`, `    const virtualExtent = BigInt(virtualSize || sizeRaw);\n    const rawAvailable = BigInt(Math.min(sizeRaw, Math.max(0, bytes.length - ptrRaw)));\n    const mappedFileSize = rawAvailable < virtualExtent ? rawAvailable : virtualExtent;\n    const perms = { read: !!(flags & 0x40000000), write: !!(flags & 0x80000000), execute: !!(flags & 0x20000000) };\n    image.addSegment({ name, address, size: virtualExtent, fileOffset: BigInt(ptrRaw), fileSize: mappedFileSize, perms, flags, source: 'PE-section' });\n    image.addSection({ name, address, size: virtualExtent, fileOffset: BigInt(ptrRaw), fileSize: mappedFileSize, perms, flags, type: null, index: i + 1, source: 'PE-section' });`);

// Permanent regression coverage in an existing binary:test entrypoint.
replace('tests/binary-platform.mjs', `import { auditBinary, openBinarySource } from '../js/binary/index.js';\nimport { makeElf64Fixture, makePe64Fixture } from './universal-binary.mjs';`, `import { BinaryImage, ByteView, auditBinary, openBinary, openBinarySource } from '../js/binary/index.js';\nimport { makeElf64Fixture, makePe64Fixture } from './universal-binary.mjs';\nimport { makeSectionlessElf64Fixture } from './universal-binary-sectionless.mjs';\nimport { parseEhFrameHeader } from '../js/binary/elf-unwind.js';`);
replace('tests/binary-platform.mjs', `console.log('binary-platform: PASS');`, `function setDynamicSize(v, size) { v.setBigUint64(64+56+32,BigInt(size),true); v.setBigUint64(64+56+40,BigInt(size),true); }\nfunction dyn(v,i,tag,val){const p=0x200+i*16;v.setBigInt64(p,BigInt(tag),true);v.setBigUint64(p+8,BigInt(val),true);}\nfunction sleb(value){let v=BigInt(value),out=[];for(;;){let byte=Number(v&0x7fn),sign=byte&0x40;v>>=7n;const done=(v===0n&&!sign)||(v===-1n&&sign);if(!done)byte|=0x80;out.push(byte);if(done)return out;}}\n\nfunction issue86To97Regressions(){\n  const relr=makeSectionlessElf64Fixture(), rv=new DataView(relr.buffer); setDynamicSize(rv,0xd0); dyn(rv,9,35,16); dyn(rv,10,36,0x4003c0); dyn(rv,11,37,8); dyn(rv,12,0,0); rv.setBigUint64(0x3c0,0x400430n,true); rv.setBigUint64(0x3c8,3n,true); const relrImage=openBinary(relr); assert.deepEqual(relrImage.relocations.filter(x=>x.source==='PT_DYNAMIC-RELR').map(x=>x.address),[0x400430n,0x400438n]);\n\n  const aps=makeSectionlessElf64Fixture(), av=new DataView(aps.buffer); setDynamicSize(av,0xc0); const apsBlob=[0x41,0x50,0x53,0x32,...sleb(1),...sleb(0x400400),...sleb(1),...sleb(15),...sleb(0x20),...sleb((1n<<32n)|7n),...sleb(5)]; aps.set(apsBlob,0x3c0); dyn(av,9,0x60000011,0x4003c0); dyn(av,10,0x60000012,apsBlob.length); dyn(av,11,0,0); const apsImage=openBinary(aps); const ar=apsImage.relocations.find(x=>x.source==='PT_DYNAMIC-ANDROID-RELA'); assert.equal(ar?.address,0x400420n); assert.equal(ar?.symbol,'puts'); assert.equal(ar?.addend,5n);\n\n  const ver=makeSectionlessElf64Fixture(), vv=new DataView(ver.buffer); const versionText=new TextEncoder().encode('GLIBC_2.2.5\\0'); ver.set(versionText,0x310); dyn(vv,2,10,0x10+versionText.length); setDynamicSize(vv,0xd0); dyn(vv,9,0x6ffffff0,0x4003c0); dyn(vv,10,0x6ffffffe,0x4003d0); dyn(vv,11,0x6fffffff,1); dyn(vv,12,0,0); vv.setUint16(0x3c0,0,true); vv.setUint16(0x3c2,2,true); vv.setUint16(0x3d0,1,true); vv.setUint16(0x3d2,1,true); vv.setUint32(0x3d4,6,true); vv.setUint32(0x3d8,16,true); vv.setUint32(0x3dc,0,true); vv.setUint32(0x3e0,0,true); vv.setUint16(0x3e4,0,true); vv.setUint16(0x3e6,2,true); vv.setUint32(0x3e8,16,true); vv.setUint32(0x3ec,0,true); const verImage=openBinary(ver); const vi=verImage.imports.find(x=>x.name==='puts'); assert.equal(vi?.version,'GLIBC_2.2.5'); assert.equal(vi?.versionLibrary,'libc.so.6');\n\n  const heuristic=makeSectionlessElf64Fixture(), hv=new DataView(heuristic.buffer); const ds=heuristic.slice(0x300,0x310); heuristic.set(ds,0x370); dyn(hv,1,5,0x400370); dyn(hv,2,10,16); dyn(hv,5,0x6ffffef0,0); dyn(hv,6,0x6ffffef1,0); dyn(hv,7,0x6ffffef2,0); dyn(hv,8,0x6ffffef3,0); const hi=openBinary(heuristic); assert.equal(hi.metadata.programDynamic.symbolCountSource,'layout-heuristic'); assert.equal(hi.metadata.programDynamicPartial,true);\n\n  const gnuBase=makeSectionlessElf64Fixture(), gnu=new Uint8Array(0x3000); gnu.set(gnuBase); const gv=new DataView(gnu.buffer); gv.setBigUint64(64+32,BigInt(gnu.length),true); gv.setBigUint64(64+40,BigInt(gnu.length),true); dyn(gv,5,0x6ffffef5,0x401000); dyn(gv,6,0x6ffffef1,0); dyn(gv,7,0x6ffffef2,0); dyn(gv,8,0x6ffffef3,0); const go=0x1000; gv.setUint32(go,64,true); gv.setUint32(go+4,1,true); gv.setUint32(go+8,1,true); gv.setUint32(go+12,0,true); let buckets=go+24; for(let i=0;i<64;i++)gv.setUint32(buckets+i*4,1,true); let chains=buckets+64*4; for(let i=0;i<100;i++)gv.setUint32(chains+i*4,i===99?1:0,true); const gi=openBinary(gnu); assert.ok(gi.warnings.some(x=>x.includes('GNU hash chain traversal exceeded')));\n\n  const internal=makeElf64Fixture(), iv=new DataView(internal.buffer); internal[0x190+5]=1; const ii=openBinary(internal); assert.equal(ii.exports.some(x=>x.name==='myfunc'),false);\n\n  const uwBytes=new Uint8Array(64), uwv=new DataView(uwBytes.buffer); const uwImage=new BinaryImage(uwBytes,{format:'elf',bits:64}); uwImage.addSection({name:'.text',address:0x1000n,size:0x20n,fileOffset:0n,fileSize:0x20n,perms:{read:true,execute:true}}); uwBytes.set([1,0xff,3,0x43],32); uwv.setUint32(36,1,true); uwv.setUint32(40,4,true); uwv.setUint32(44,0,true); parseEhFrameHeader(new ByteView(uwBytes),{addr:0x2000n,offset:32n,size:16n},uwImage,64); assert.ok(uwImage.warnings.some(x=>x.includes('funcrel requires a function base')));\n  const indirectImage=new BinaryImage(uwBytes,{format:'elf',bits:64}); indirectImage.addSection({name:'.text',address:0x1000n,size:0x20n,fileOffset:0n,fileSize:0x20n,perms:{read:true,execute:true}}); uwBytes.set([1,0xff,3,0x83],32); uwv.setUint32(36,1,true); uwv.setUint32(40,0xdead,true); uwv.setUint32(44,0,true); parseEhFrameHeader(new ByteView(uwBytes),{addr:0x2000n,offset:32n,size:16n},indirectImage,64); assert.ok(indirectImage.warnings.some(x=>x.includes('indirect target')));\n\n  const shortPe=makePe64Fixture(), spv=new DataView(shortPe.buffer); spv.setUint16(0x84+16,0x60,true); assert.throws(()=>openBinary(shortPe),/optional header size/);\n  const pe=openBinary(makePe64Fixture()); const text=pe.sections.find(s=>s.name==='.text'); assert.equal(text.size,0x100n); assert.equal(text.fileSize,0x100n); assert.equal(pe.addressToOffset(0x140001100n),null); const textSeg=pe.segments.find(s=>s.name==='.text'); assert.equal(textSeg.size,0x100n); assert.equal(textSeg.fileSize,0x100n);\n  const noEntry=makePe64Fixture(), nev=new DataView(noEntry.buffer); nev.setUint32(0x84+20+16,0,true); const nei=openBinary(noEntry); assert.equal(nei.entrypoint,null); assert.equal(nei.functions.some(f=>f.source==='entrypoint'),false);\n}\n\nissue86To97Regressions();\nconsole.log('binary-platform: PASS');`);

fs.rmSync('tools/fix-issues-86-97.mjs');
fs.rmSync('.github/workflows/fix-issues-86-97.yml');
