name: ChatGPT userscript host

on:
  pull_request:
    paths:
      - 'js/userscript/**'
      - 'js/ai/provider/**'
      - 'js/ai/ui/bridge.js'
      - 'scripts/build-userscript.mjs'
      - 'userscript/**'
      - 'worker-entry.js'
      - 'wrangler.jsonc'
      - 'package.json'
      - 'package-lock.json'
      - 'index.html'
      - 'css/**'
      - 'tests/userscript-*.mjs'
      - '.github/workflows/userscript-host.yml'
  push:
    branches: [main]
    paths:
      - 'js/userscript/**'
      - 'js/ai/provider/**'
      - 'js/ai/ui/bridge.js'
      - 'scripts/build-userscript.mjs'
      - 'userscript/**'
      - 'worker-entry.js'
      - 'wrangler.jsonc'
      - 'package.json'
      - 'package-lock.json'
      - 'index.html'
      - 'css/**'
      - 'tests/userscript-*.mjs'
      - '.github/workflows/userscript-host.yml'

permissions:
  contents: write

jobs:
  userscript:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Opaque sandbox storage contract
        run: node tests/userscript-sandbox-storage.mjs
      - name: Regenerate committed userscript loader
        run: |
          npm run userscript:build
          if ! git diff --quiet -- userscript/hex.user.template.js; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add userscript/hex.user.template.js
            git commit -m "Regenerate userscript loader"
            git push origin HEAD:${GITHUB_HEAD_REF}
          fi
      - name: Build and userscript tests
        run: npm run userscript:test
      - name: AI and worker regressions
        run: |
          npm run ai:test
          node tests/gemini.mjs
          node tests/worker-classic-harness.mjs
      - name: Verify tiny committed loader is current
        run: git diff --exit-code -- userscript/hex.user.template.js

  embed-browser:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Build secure userscript runtime
        run: npm run userscript:build
      - name: Install pinned browser engines
        run: |
          npm install --no-save --save-exact playwright@1.55.0
          npx --no-install playwright install-deps chromium webkit
          npx --no-install playwright install chromium webkit
      - name: Verify opaque srcdoc module and storage execution
        run: node tests/userscript-srcdoc-probe.mjs
      - name: Run full secure sandbox startup
        run: node tests/userscript-sandbox-browser-e2e.mjs