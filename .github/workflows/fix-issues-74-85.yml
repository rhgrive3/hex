name: Apply issues 74-85 ELF hardening
on:
  push:
    branches: [fix/issues-74-85]
permissions:
  contents: write
jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply exact patch
        run: node tools/fix-issues-74-85.mjs
      - name: Syntax check
        run: |
          node --check js/binary/elf.js
          node --check js/binary/elf-dynamic.js
          node --check tests/universal-binary-sectionless.mjs
      - name: Binary regressions
        run: npm run binary:test
      - name: Full regression gate
        run: npm run check
      - name: Commit verified fix
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m "Fix ELF issues 74-85"
          git push origin HEAD:fix/issues-74-85
