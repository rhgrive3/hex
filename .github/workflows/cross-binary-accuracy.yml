name: Cross-binary analysis parked

# PR #999 is a disposable investigation branch. Automatic runs are disabled
# after the evidence collection completed. Re-enable manually only if another
# isolated experiment is needed.
on:
  workflow_dispatch:

jobs:
  parked:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cross-binary investigation is parked"
