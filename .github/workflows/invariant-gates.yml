name: Invariant Gates (disabled for cross-binary analysis)

# Disposable analysis branch: unrelated invariant CI is intentionally disabled.
# The production workflow remains untouched on every non-analysis branch.
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled on analysis/cross-binary-100pct"
