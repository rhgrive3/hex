name: One-shot repair runner 890-895
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
permissions:
  contents: write
jobs:
  repair:
    if: github.event_name == 'push' || github.event.pull_request.head.ref == 'fix/issues-890-895-control-profile-20260819'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/issues-890-895-control-profile-20260819
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Rebase target onto current main
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git fetch origin main
          git rebase origin/main
      - name: Apply exact guarded repair and corrected fixtures
        run: |
          python3 tools/repair-issues-890-895.py
          python3 tools/repair-issues-890-895-testfix.py
      - name: Focused regressions
        run: node --test tests/phase6/generic-core/issues-890-895.test.mjs
      - name: Regenerate canonical userscript before sync gates
        run: npm run userscript:build
      - name: Repository verification
        run: npm run check
      - name: Canonical userscript idempotence
        run: |
          cp userscript/hex.user.js /tmp/hex.user.js
          cp userscript/hex.user.template.js /tmp/hex.user.template.js
          npm run userscript:build
          cmp userscript/hex.user.js /tmp/hex.user.js
          cmp userscript/hex.user.template.js /tmp/hex.user.template.js
      - name: Commit verified source result
        run: |
          rm -f tools/repair-issues-890-895.py tools/repair-issues-890-895-testfix.py
          rm -f .github/workflows/repair-issues-890-895.yml .github/workflows/tmp-repair-runner-890-895.yml
          git add -A
          git diff --cached --check
          git commit -m 'fix: close control and profile correctness issues 890-895'
          git push --force-with-lease origin HEAD:fix/issues-890-895-control-profile-20260819
# trigger corrected ordering
